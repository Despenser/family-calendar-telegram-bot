# План реализации: Фильтрация удаленных событий в списке "Мои события"

## Обзор

Данный план описывает пошаговую реализацию фильтрации удаленных событий в команде `/my_events`. Реализация включает добавление нового метода в репозиторий, обновление сервисного слоя и написание тестов для проверки корректности.

## Задачи

- [x] 1. Добавить метод фильтрации в EventRepository
  - Добавить метод `findByUserIdAndStatusOrderByEventDateAsc` с аннотацией `@EntityGraph`
  - Добавить JavaDoc документацию к методу
  - _Требования: 1.1, 3.1, 3.2_

- [ ]* 1.1 Написать property-тест для нового метода репозитория
  - **Свойство 1: Фильтрация активных событий**
  - **Проверяет: Требования 1.1, 3.1, 3.2, 3.3**
  - Генерировать случайного пользователя с событиями разных статусов
  - Вызывать новый метод репозитория с статусом ACTIVE
  - Проверять, что все возвращенные события имеют статус ACTIVE

- [x] 2. Обновить метод getUserEvents в EventService
  - Изменить вызов репозитория на новый метод с фильтрацией по статусу ACTIVE
  - Обновить JavaDoc документацию метода
  - Обновить логирование для указания фильтрации по статусу
  - _Требования: 1.1, 1.2, 1.3, 1.4, 3.1, 3.2, 3.3_

- [ ]* 2.1 Написать property-тест для метода getUserEvents
  - **Свойство 2-4: Исключение событий с другими статусами**
  - **Проверяет: Требования 1.2, 1.3, 1.4**
  - Генерировать случайного пользователя с событиями всех статусов
  - Вызывать `getUserEvents()`
  - Проверять, что ни одно событие не имеет статус DELETED, DRAFT или COMPLETED

- [ ]* 2.2 Написать unit-тесты для метода getUserEvents
  - Тест для пустого списка событий
  - Тест для пользователя только с активными событиями
  - Тест для пользователя со смешанными статусами событий
  - Тест для проверки сортировки по дате
  - _Требования: 1.1, 1.2, 1.3, 1.4_

- [x] 3. Контрольная точка - Убедиться, что все тесты проходят
  - Убедиться, что все тесты проходят, спросить пользователя, если возникнут вопросы.

- [ ]* 4. Написать integration-тест для команды /my_events
  - Создать пользователя с событиями разных статусов
  - Вызвать команду `/my_events`
  - Проверить, что отображаются только активные события
  - _Требования: 1.1, 1.2, 1.3, 1.4_

- [ ]* 5. Проверить корректность работы корзины
  - **Свойство 5: Корзина содержит только удаленные события**
  - **Проверяет: Требования 2.1**
  - Написать property-тест для TrashService
  - Генерировать случайного пользователя с событиями разных статусов
  - Вызывать метод получения корзины
  - Проверять, что все возвращенные события имеют статус DELETED

- [x] 6. Финальная контрольная точка - Убедиться, что все тесты проходят
  - Убедиться, что все тесты проходят, спросить пользователя, если возникнут вопросы.

## Примечания

- Задачи, помеченные `*`, являются опциональными и могут быть пропущены для более быстрого MVP
- Каждая задача ссылается на конкретные требования для отслеживаемости
- Контрольные точки обеспечивают инкрементальную валидацию
- Property-тесты проверяют универсальные свойства корректности
- Unit-тесты проверяют конкретные примеры и граничные случаи
