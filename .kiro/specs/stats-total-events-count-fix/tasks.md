# Implementation Plan: Stats Total Events Count Fix

## Overview

Исправление подсчета "Всего событий" в статистике. Изменение логики в StatisticsService для подсчета суммы активных и завершенных событий, и удаление текста "(только активные)" из вывода StatsCommandHandler.

## Tasks

- [x] 1. Исправить логику подсчета totalEvents в StatisticsService
  - Изменить метод `getMonthlyStatistics()` для подсчета суммы активных и завершенных событий
  - Убедиться, что activeEvents считается отдельно перед подсчетом totalEvents
  - Обновить комментарии в коде для отражения новой логики
  - _Requirements: 1.1_

- [ ]* 1.1 Написать unit тесты для StatisticsService
  - Тест с только активными событиями
  - Тест с только завершенными событиями
  - Тест со смешанными событиями
  - Тест с нулевым количеством событий
  - _Requirements: 1.1_

- [ ]* 1.2 Написать property тест для суммы событий
  - **Property 1: Total events equals sum of active and completed**
  - **Validates: Requirements 1.1**

- [ ]* 1.3 Написать property тест для максимального значения
  - **Property 2: Total events is never less than active or completed**
  - **Validates: Requirements 1.1**

- [x] 2. Удалить текст "(только активные)" из StatsCommandHandler
  - Найти строку с текстом "(только активные)" в методе `handle()`
  - Удалить вызов `.append(escape(" (только активные)"))`
  - Обновить JavaDoc комментарии для отражения новой логики
  - _Requirements: 1.3_

- [ ]* 2.1 Написать unit тест для вывода StatsCommandHandler
  - Проверить отсутствие текста "(только активные)" в выводе
  - Проверить корректное отображение totalEvents
  - _Requirements: 1.3_

- [x] 3. Checkpoint - Убедиться, что все тесты проходят
  - Запустить все тесты проекта
  - Исправить любые найденные проблемы
  - Убедиться, что изменения не сломали существующую функциональность

## Notes

- Задачи, помеченные `*`, являются опциональными и могут быть пропущены для более быстрой реализации MVP
- Каждая задача ссылается на конкретные требования для отслеживаемости
- Checkpoint задачи обеспечивают инкрементальную валидацию
- Property тесты валидируют универсальные свойства корректности
- Unit тесты валидируют конкретные примеры и граничные случаи
