# План реализации: event-creation-header-consistency

## Обзор

Добавление единой жирной шапки "📅 Создание нового события" во все сообщения процесса создания события для обеспечения визуальной согласованности интерфейса.

## Задачи

- [x] 1. Добавить константу для шапки создания события
  - Добавить константу `EVENT_CREATION_HEADER` в класс `BotMessageBuilder`
  - Значение: "📅 Создание нового события"
  - _Требования: 1.4_

- [x] 2. Обновить методы buildTimeSelectedMessage
  - [x] 2.1 Обновить метод buildTimeSelectedMessage(LocalTime time)
    - Добавить шапку в начало сообщения: `bold(EVENT_CREATION_HEADER) + "\n\n"`
    - Сохранить существующее содержимое сообщения
    - _Требования: 1.1, 1.2, 1.3, 2.1_
  
  - [x] 2.2 Обновить метод buildTimeSelectedMessage(String formattedTime)
    - Добавить шапку в начало сообщения: `bold(EVENT_CREATION_HEADER) + "\n\n"`
    - Сохранить существующее содержимое сообщения
    - _Требования: 1.1, 1.2, 1.3, 2.1_

- [x] 3. Обновить существующие unit-тесты
  - Обновить тесты в `BotMessageBuilderPropertyTest` или других тестовых классах
  - Проверить, что тесты учитывают добавленную шапку
  - Убедиться, что все существующие тесты проходят
  - _Требования: 2.3, 3.1_

- [ ]* 4. Написать unit-тесты для проверки шапки
  - [ ]* 4.1 Тест: buildTimeSelectedMessage содержит шапку
    - Проверить, что метод возвращает сообщение, начинающееся с шапки
    - Проверить точное содержимое шапки "📅 Создание нового события"
    - _Требования: 1.1, 1.4_
  
  - [ ]* 4.2 Тест: шапка отформатирована жирным шрифтом
    - Проверить, что шапка обернута в метод bold()
    - Проверить наличие "\n\n" после шапки
    - _Требования: 1.2, 1.3_
  
  - [ ]* 4.3 Тест: сообщение содержит время и запрос названия
    - Проверить, что сообщение содержит переданное время
    - Проверить наличие текста "Теперь отправьте название события"
    - _Требования: 2.2_

- [ ]* 5. Написать property-based тесты
  - [ ]* 5.1 Property-тест для свойства 1
    - **Свойство 1: Все сообщения создания события содержат шапку**
    - **Валидирует: Требования 1.1**
    - Генерировать случайные значения времени (LocalTime)
    - Проверять, что buildTimeSelectedMessage() возвращает сообщение, начинающееся с шапки
    - Минимум 100 итераций
  
  - [ ]* 5.2 Property-тест для свойства 2
    - **Свойство 2: Шапка отформатирована жирным шрифтом**
    - **Валидирует: Требования 1.2**
    - Генерировать случайные значения времени
    - Проверять, что шапка содержит Markdown форматирование для жирного текста
    - Минимум 100 итераций
  
  - [ ]* 5.3 Property-тест для свойства 3
    - **Свойство 3: После шапки следует пустая строка**
    - **Валидирует: Требования 1.3**
    - Генерировать случайные значения времени
    - Проверять, что после шапки следует "\n\n"
    - Минимум 100 итераций
  
  - [ ]* 5.4 Property-тест для свойства 4
    - **Свойство 4: Сообщение после выбора времени содержит время и запрос названия**
    - **Валидирует: Требования 2.2**
    - Генерировать случайные значения времени
    - Проверять, что сообщение содержит отформатированное время
    - Проверять, что сообщение содержит текст запроса названия
    - Минимум 100 итераций

- [x] 6. Контрольная точка - убедиться, что все тесты проходят
  - Убедиться, что все тесты проходят, задать вопросы пользователю при необходимости

## Примечания

- Задачи, помеченные `*`, являются опциональными и могут быть пропущены для более быстрого MVP
- Каждая задача ссылается на конкретные требования для отслеживаемости
- Контрольная точка обеспечивает инкрементальную валидацию
- Property-тесты валидируют универсальные свойства корректности
- Unit-тесты валидируют конкретные примеры и граничные случаи
