# План реализации

- [x] 1. Обновить HelpCommandHandler для кликабельных команд





  - Открыть файл `src/main/java/ru/golubyatnikov/family/calendar/bot/handler/HelpCommandHandler.java`
  - В методе `buildCommandsList()` заменить `code(handler.getCommand())` на `escape(handler.getCommand())`
  - Проверить, что команды отображаются в формате `/command` без backticks
  - Убедиться, что описания команд корректно экранируются
  - Обновить Javadoc с информацией о кликабельности команд
  - _Требования: 1.1, 1.2, 1.3, 1.4, 1.5, 2.1, 2.2, 2.5, 3.1, 3.2, 3.3, 3.4, 3.5_

- [ ]* 1.1 Написать unit тест для кликабельности команд в /help
  - **Свойство 1: Команды не используют моноширинное форматирование**
  - **Проверяет: Требования 1.1, 2.5**

- [ ]* 1.2 Написать unit тест для сортировки команд
  - **Свойство 4: Команды отсортированы в алфавитном порядке**
  - **Проверяет: Требования 1.5**

- [x] 2. Обновить ContextualHintsService для кликабельных команд





  - Открыть файл `src/main/java/ru/golubyatnikov/family/calendar/bot/service/ContextualHintsService.java`
  - Найти все использования `code()` для команд (текст, начинающийся с `/`)
  - Заменить `code("/command")` на `escape("/command")` для всех команд
  - Оставить `code()` для технической информации (не команд)
  - Проверить метод `getHint()` и все приватные методы формирования подсказок
  - Убедиться, что все команды в подсказках кликабельны
  - _Требования: 4.1, 4.2, 4.3, 4.4, 4.5, 8.2, 8.3_

- [ ]* 2.1 Написать unit тест для кликабельности команд в подсказках
  - Проверить, что команды в подсказках НЕ используют `code()` форматирование
  - Проверить, что все упомянутые команды в формате `/command`
  - _Требования: 4.1, 4.2_


- [x] 3. Обновить StartCommandHandler для кликабельных команд




  - Открыть файл `src/main/java/ru/golubyatnikov/family/calendar/bot/handler/StartCommandHandler.java`
  - Найти все использования `code()` для команд в приветственном сообщении
  - Заменить `code("/command")` на `escape("/command")` для всех команд
  - Проверить, что все команды в приветствии кликабельны
  - Обновить Javadoc с информацией о кликабельности команд
  - _Требования: 6.1, 6.2, 6.3, 6.4, 6.5, 8.2_

- [ ]* 3.1 Написать unit тест для кликабельности команд в приветствии
  - Проверить, что команды в приветствии кликабельны
  - Проверить, что все упомянутые команды в формате `/command`
  - _Требования: 6.1, 6.2_


- [x] 4. Проверить и обновить TodayCommandHandler




  - Открыть файл `src/main/java/ru/golubyatnikov/family/calendar/bot/handler/TodayCommandHandler.java`
  - Найти все использования `code()` для команд (если есть)
  - Заменить `code("/command")` на `escape("/command")` для всех команд
  - Оставить `code()` для технической информации (не команд)
  - Проверить сообщения об отсутствии событий на наличие упоминаний команд
  - _Требования: 7.1, 7.2, 7.3, 7.4, 8.2_


- [x] 5. Проверить и обновить WeekCommandHandler




  - Открыть файл `src/main/java/ru/golubyatnikov/family/calendar/bot/handler/WeekCommandHandler.java`
  - Найти все использования `code()` для команд (если есть)
  - Заменить `code("/command")` на `escape("/command")` для всех команд
  - Проверить сообщения об отсутствии событий на наличие упоминаний команд
  - _Требования: 7.1, 7.2, 7.3, 7.4, 8.2_

- [x] 6. Проверить и обновить SearchCommandHandler





  - Открыть файл `src/main/java/ru/golubyatnikov/family/calendar/bot/handler/SearchCommandHandler.java`
  - Найти все использования `code()` для команд (если есть)
  - Заменить `code("/command")` на `escape("/command")` для всех команд
  - Проверить сообщения об отсутствии результатов на наличие упоминаний команд
  - _Требования: 7.1, 7.2, 7.3, 7.4, 8.2_





- [x] 7. Проверить и обновить остальные обработчики команд

  - Проверить FilterCommandHandler на наличие упоминаний команд
  - Проверить StatsCommandHandler на наличие упоминаний команд
  - Проверить TrashCommandHandler на наличие упоминаний команд
  - Проверить MyEventsCommandHandler на наличие упоминаний команд








  - Проверить UpcomingEventsCommandHandler на наличие упоминаний команд
  - Заменить `code("/command")` на `escape("/command")` для всех найденных команд
  - Оставить `code()` для технической информации (не команд)
  - _Требования: 7.1, 7.2, 7.3, 7.4, 8.2, 8.5_




- [ ] 8. Checkpoint - Убедиться, что все тесты проходят

  - Убедиться, что все тесты проходят, спросить пользователя, если возникнут вопросы

- [x] 9. Проверить и обновить сообщения об ошибках





  - Проверить GlobalExceptionHandler на наличие упоминаний команд
  - Проверить ValidationExceptionHandler на наличие упоминаний команд (если существует)
  - Проверить методы валидации в сервисах на наличие упоминаний команд
  - Заменить `code("/command")` на `escape("/command")` для всех команд в сообщениях об ошибках
  - Убедиться, что команда `/start` в сообщениях об авторизации кликабельна
  - _Требования: 5.1, 5.2, 5.3, 5.4, 5.5, 8.2_

- [ ] 10. Проверить и обновить планировщики

  - Проверить WeeklySummaryScheduler на наличие упоминаний команд
  - Проверить EventCompletionScheduler на наличие упоминаний команд
  - Проверить ReminderService на наличие упоминаний команд (если реализован)
  - Заменить `code("/command")` на `escape("/command")` для всех найденных команд
  - _Требования: 7.1, 7.2, 7.3, 7.4, 8.2_

- [ ] 11. Финальный checkpoint - Убедиться, что все тесты проходят

  - Убедиться, что все тесты проходят, спросить пользователя, если возникнут вопросы

- [ ]* 12. Ручное тестирование
  - Отправить команду `/help` и проверить, что все команды кликабельны
  - Добавить событие и проверить, что команды в подсказках кликабельны
  - Отправить команду `/start` и проверить, что команды в приветствии кликабельны
  - Проверить команды `/today`, `/week`, `/search` на наличие кликабельных упоминаний других команд
  - Вызвать ошибку и проверить, что команды в сообщениях об ошибках кликабельны
  - Проверить, что техническая информация (не команды) по-прежнему отображается моноширинным шрифтом
  - Убедиться, что все изменения работают корректно в реальном Telegram-клиенте

