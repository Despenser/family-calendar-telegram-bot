# План реализации: Реорганизация расположения кнопок под событиями

## Обзор

Данный план описывает пошаговую реализацию изменений в методе `createEventActionsKeyboard` класса `KeyboardService` для реорганизации кнопок управления событиями из трех рядов в два ряда с более логичной группировкой.

## Задачи

- [x] 1. Модифицировать метод createEventActionsKeyboard для нового расположения кнопок
  - Изменить логику создания второго ряда кнопок
  - Переместить кнопку "Вложения" из отдельного ряда во второй ряд
  - Переместить кнопку "Завершить" из третьего ряда во второй ряд (рядом с "Вложения")
  - Изменить текст кнопки завершения с "✅ Завершить событие" на "✅ Завершить"
  - Сохранить все callback data без изменений
  - Сохранить логику условного отображения кнопки "Завершить"
  - Обновить логирование для отражения новой структуры
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5, 2.1, 2.2, 2.3, 3.1, 3.2, 3.3_

- [ ]* 1.1 Написать property-тест для Property 1
  - **Property 1: Клавиатура всегда содержит два ряда**
  - **Validates: Requirements 1.1**

- [ ]* 1.2 Написать property-тест для Property 2
  - **Property 2: Первый ряд содержит кнопки редактирования и удаления**
  - **Validates: Requirements 1.2, 3.1**

- [ ]* 1.3 Написать property-тест для Property 3
  - **Property 3: Второй ряд содержит кнопку вложений и условно кнопку завершения**
  - **Validates: Requirements 1.3, 1.4, 1.5, 2.1, 2.2, 2.3, 3.1, 3.3**

- [ ]* 1.4 Написать property-тест для Property 4
  - **Property 4: Счетчик вложений отображается корректно**
  - **Validates: Requirements 3.2**

- [ ]* 1.5 Написать property-тест для Property 5
  - **Property 5: Callback data имеют правильный формат**
  - **Validates: Requirements 3.1**

- [x] 2. Обновить существующие unit-тесты в KeyboardServiceTest
  - Обновить тест `shouldCreateEventActionsKeyboard` для проверки двух рядов
  - Обновить проверки структуры первого ряда (без изменений)
  - Добавить проверки структуры второго ряда для активного события владельца
  - Добавить проверки структуры второго ряда для неактивного события
  - Добавить проверки структуры второго ряда для события другого пользователя
  - Проверить новый текст кнопки "✅ Завершить" (без слова "событие")
  - Проверить, что все callback data остались неизменными
  - Обновить проверки счетчика вложений
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5, 2.1, 2.2, 2.3, 3.1, 3.2, 4.1, 4.2, 4.3, 4.4_

- [x] 3. Checkpoint - Убедиться, что все тесты проходят
  - Запустить все unit-тесты KeyboardServiceTest
  - Запустить все property-тесты (если реализованы)
  - Убедиться, что не сломаны другие тесты, использующие KeyboardService
  - Спросить пользователя, если возникнут вопросы

## Примечания

- Задачи, помеченные `*`, являются опциональными и могут быть пропущены для более быстрой реализации MVP
- Каждая задача ссылается на конкретные требования для отслеживаемости
- Property-тесты валидируют универсальные свойства корректности
- Unit-тесты валидируют конкретные примеры и граничные случаи
- Все изменения локализованы в одном методе, что минимизирует риск регрессии
