# План реализации: Переупорядочивание списка при завершении события

## Обзор

Реализация функциональности автоматического переупорядочивания списка "Мои события" при завершении события, которое находится не в конце списка. Завершённое событие перемещается вниз, активные события остаются выше.

## Задачи

- [x] 1. Добавить вспомогательные методы в EventService
  - Создать метод `findEventPosition()` для определения позиции события в списке
  - Создать метод `deleteActiveEventMessages()` для удаления сообщений активных событий
  - Создать метод `sendEventWithHeader()` для отправки события с шапкой
  - Создать метод `sendEvent()` для отправки события без шапки
  - Создать метод `sendCompletedEventWithNote()` для отправки завершённого события с предложением добавить заметку
  - _Requirements: 1.1, 1.2, 4.2_

- [ ]* 1.1 Написать unit-тесты для вспомогательных методов
  - Тест `findEventPosition()` с различными позициями события
  - Тест `deleteActiveEventMessages()` с успешным и неуспешным удалением
  - Тест `sendEventWithHeader()` с корректной отправкой
  - _Requirements: 1.1, 1.2, 4.2_

- [x] 2. Реализовать метод reorderMyEventsList() в EventService
  - Получить актуальный список активных событий
  - Удалить все сообщения активных событий через `deleteActiveEventMessages()`
  - Вызвать `resendMyEventsWithHeader()` для отправки событий в новом порядке
  - Добавить логирование всех шагов
  - _Requirements: 1.1, 1.2, 1.4, 4.2_

- [ ]* 2.1 Написать unit-тесты для reorderMyEventsList()
  - Тест успешного переупорядочивания списка
  - Тест обновления messageId для всех событий
  - Тест обработки ошибок при удалении сообщений
  - _Requirements: 1.1, 1.2, 1.4, 4.2_

- [x] 3. Реализовать метод resendMyEventsWithHeader() в EventService
  - Сформировать шапку с учётом всех событий (активные + завершённое)
  - Отправить шапку + первое активное событие (если есть активные)
  - Отправить остальные активные события
  - Отправить завершённое событие с предложением добавить заметку
  - Сохранить новые messageId для всех событий
  - _Requirements: 1.1, 1.2, 1.5, 4.2_

- [ ]* 3.1 Написать unit-тесты для resendMyEventsWithHeader()
  - Тест отправки с активными событиями
  - Тест отправки только завершённого события
  - Тест корректности счётчика в шапке
  - Тест сохранения messageId
  - _Requirements: 1.1, 1.2, 1.5, 4.2_

- [x] 4. Реализовать метод completeEventWithReordering() в EventService
  - Получить событие и проверить права доступа
  - Получить список активных событий ДО завершения
  - Определить позицию события в списке через `findEventPosition()`
  - Проверить, является ли событие последним
  - Завершить событие через `completeEventWithoutHeaderUpdate()`
  - Если событие не последнее и есть другие события - вызвать `reorderMyEventsList()`
  - Добавить подробное логирование
  - _Requirements: 1.1, 1.2, 1.3, 1.4_

- [ ]* 4.1 Написать unit-тесты для completeEventWithReordering()
  - Тест завершения не последнего события с переупорядочиванием
  - Тест завершения последнего события без переупорядочивания
  - Тест завершения единственного события
  - Тест проверки прав доступа
  - _Requirements: 1.1, 1.2, 1.3, 1.4_

- [ ]* 4.2 Написать property-тест для сохранения порядка активных событий
  - **Свойство 2: Сохранение порядка активных событий**
  - **Validates: Requirements 1.4**
  - Генерация случайных списков событий (2-10 событий)
  - Завершение случайного события (не последнего)
  - Проверка сохранения относительного порядка активных событий
  - _Requirements: 1.4_

- [ ]* 4.3 Написать property-тест для обновления messageId
  - **Свойство 3: Обновление messageId после переупорядочивания**
  - **Validates: Requirements 4.2**
  - Генерация случайных списков событий
  - Завершение случайного события с переупорядочиванием
  - Проверка, что все messageId обновлены и не null
  - _Requirements: 4.2_

- [ ]* 4.4 Написать property-тест для сохранения данных события
  - **Свойство 5: Сохранение данных события**
  - **Validates: Requirements 4.4**
  - Генерация случайных событий с различными данными
  - Завершение события с переупорядочиванием
  - Проверка неизменности всех полей события (title, description, date, time)
  - _Requirements: 4.4_

- [ ] 5. Checkpoint - Убедиться, что все тесты проходят
  - Убедиться, что все тесты проходят, спросить пользователя, если возникнут вопросы.

- [x] 6. Обновить EventCallbackHandler.handleCompleteEvent()
  - Заменить вызов `completeEventWithoutHeaderUpdate()` на `completeEventWithReordering()`
  - Удалить код редактирования текущего сообщения (оно будет пересоздано)
  - Обновить сохранение контекста для использования нового messageId из completedEvent
  - Обновить JavaDoc и комментарии
  - _Requirements: 1.1, 1.2, 2.1_

- [ ]* 6.1 Написать unit-тесты для обновлённого handleCompleteEvent()
  - Тест вызова `completeEventWithReordering()`
  - Тест сохранения контекста с обновлённым messageId
  - Тест обработки ошибок
  - _Requirements: 1.1, 1.2, 2.1_

- [x] 7. Обновить UpdateProcessor для обработки заметок
  - Убедиться, что после добавления заметки вызывается `updateMyEventsHeaderAfterRemoval()`
  - Проверить корректность обработки пропуска заметки
  - Обновить комментарии и логирование
  - _Requirements: 2.2, 2.3_

- [ ]* 7.1 Написать unit-тесты для обработки заметок
  - Тест добавления заметки с последующим обновлением шапки
  - Тест пропуска заметки с обновлением шапки
  - _Requirements: 2.2, 2.3_

- [x] 8. Checkpoint - Убедиться, что все тесты проходят
  - Убедиться, что все тесты проходят, спросить пользователя, если возникнут вопросы.

- [ ] 9. Интеграционное тестирование
  - Создать интеграционный тест для полного цикла завершения с переупорядочиванием
  - Проверить взаимодействие всех компонентов
  - Проверить корректность обновления базы данных
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5, 2.1, 2.2, 2.3, 4.1, 4.2, 4.3, 4.4_

- [ ] 10. Финальный checkpoint - Убедиться, что все тесты проходят
  - Убедиться, что все тесты проходят, спросить пользователя, если возникнут вопросы.

## Примечания

- Задачи, отмеченные `*`, являются опциональными и могут быть пропущены для более быстрого MVP
- Каждая задача ссылается на конкретные требования для отслеживаемости
- Checkpoint задачи обеспечивают инкрементальную валидацию
- Property-тесты валидируют универсальные свойства корректности
- Unit-тесты валидируют конкретные примеры и граничные случаи
