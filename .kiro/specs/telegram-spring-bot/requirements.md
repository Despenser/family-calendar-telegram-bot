# Документ требований

## Введение

Данный проект представляет собой Spring Boot 3.5.3 приложение, интегрированное с Telegram Bot API для создания семейного календаря. Система позволяет членам семьи планировать события, получать уведомления и просматривать расписание через Telegram бота. Бот использует Webhook для получения обновлений, PostgreSQL для хранения данных и Docker для развертывания.

## Глоссарий

- **Система**: Spring Boot приложение с интеграцией Telegram Bot API и PostgreSQL
- **Telegram Bot API**: Официальный API Telegram для создания ботов
- **Пользователь**: Член семьи, взаимодействующий с ботом через Telegram
- **Команда**: Текстовое сообщение, начинающееся с символа "/" (например, /start, /help)
- **Webhook**: HTTP endpoint для получения обновлений от Telegram в реальном времени
- **Update**: Объект, содержащий информацию о новом событии (сообщение, команда, callback)
- **Событие**: Запланированная активность с датой, временем и описанием
- **Семья**: Группа пользователей, имеющих доступ к общему календарю
- **TelegramId**: Уникальный идентификатор пользователя в Telegram
- **Maven**: Система управления зависимостями и сборки проекта
- **Git**: Система контроля версий
- **Docker**: Платформа контейнеризации приложений
- **PostgreSQL**: Реляционная база данных для хранения событий и пользователей
- **Персональное событие**: Событие, видимое только создателю
- **Семейное событие**: Событие, видимое всем членам семьи
- **Повторяющееся событие**: Событие с настроенным расписанием повторений
- **Серия событий**: Группа связанных повторяющихся событий
- **Вложение**: Файл, прикрепленный к событию (билет, документ, изображение)
- **Чек-лист**: Список задач внутри события с возможностью отметки выполнения
- **Комментарий**: Текстовая заметка к событию от члена семьи
- **Корзина**: Временное хранилище удаленных событий для возможности восстановления
- **История изменений**: Журнал всех изменений события с указанием автора и времени

## Требования

### Требование 1

**Пользовательская история:** Как разработчик, я хочу иметь правильно настроенную структуру Spring Boot 3.5.3 проекта с Maven, PostgreSQL и Docker, чтобы начать разработку семейного календарь-бота с правильными зависимостями и конфигурацией.

#### Критерии приемки

1. WHEN проект создается, THEN Система ДОЛЖНА включать корректный pom.xml с зависимостями Spring Boot 3.5.3, telegrambots-spring-boot-starter 8.2.0, Spring Data JPA, PostgreSQL driver, Flyway 11.1.0, Testcontainers 1.21.2 и необходимыми плагинами Maven
2. WHEN проект инициализируется, THEN Система ДОЛЖНА содержать правильную структуру пакетов согласно best practices Java (controller, service, repository, model, config)
3. WHEN приложение запускается, THEN Система ДОЛЖНА успешно загружать Spring контекст и подключаться к PostgreSQL без ошибок
4. WHEN конфигурация читается, THEN Система ДОЛЖНА загружать параметры бота, базы данных и webhook URL из application.properties или переменных окружения
5. WHERE используется Docker, Система ДОЛЖНА включать Dockerfile и docker-compose.yml для контейнеризации приложения и PostgreSQL

### Требование 2

**Пользовательская история:** Как разработчик, я хочу безопасно хранить и загружать конфигурацию бота и базы данных, чтобы токены, пароли и секреты не попадали в систему контроля версий.

#### Критерии приемки

1. WHEN приложение запускается, THEN Система ДОЛЖНА читать токен бота, webhook URL и параметры БД из переменных окружения или application.properties
2. WHEN обязательные параметры отсутствуют, THEN Система ДОЛЖНА выбросить исключение с понятным сообщением об ошибке и остановить запуск
3. WHEN используется Git, THEN Система ДОЛЖНА исключать файлы с конфиденциальными данными через .gitignore
4. WHERE используются профили Spring, Система ДОЛЖНА поддерживать различные конфигурации для dev, test и prod окружений
5. WHEN конфигурация валидируется, THEN Система ДОЛЖНА проверять наличие всех обязательных параметров при старте приложения

### Требование 3

**Пользовательская история:** Как член семьи, я хочу зарегистрироваться в боте по моему Telegram ID, чтобы получить доступ к семейному календарю.

#### Критерии приемки

1. WHEN пользователь отправляет команду /start, THEN Система ДОЛЖНА проверить наличие пользователя в базе данных по Telegram ID
2. WHEN пользователь не найден в базе, THEN Система ДОЛЖНА отправить сообщение о необходимости регистрации или отсутствии доступа
3. WHEN пользователь найден в базе, THEN Система ДОЛЖНА отправить приветственное сообщение с информацией о доступных командах
4. WHEN регистрация выполняется, THEN Система ДОЛЖНА сохранить Telegram ID, имя пользователя и дату регистрации в PostgreSQL
5. WHEN команда обрабатывается, THEN Система ДОЛЖНА логировать событие с информацией о пользователе

### Требование 4

**Пользовательская история:** Как член семьи, я хочу создать событие в календаре с датой, временем и описанием через интерактивный интерфейс с inline-кнопками, чтобы процесс был удобным и интуитивным.

#### Критерии приемки

1. WHEN пользователь отправляет команду /add_event, THEN Система ДОЛЖНА отобразить inline-календарь с кнопками для выбора даты
2. WHEN пользователь выбирает дату из календаря, THEN Система ДОЛЖНА отобразить inline-кнопки для выбора временного интервала (часы и минуты)
3. WHEN пользователь выбирает время, THEN Система ДОЛЖНА запросить ввод названия события через текстовое сообщение
4. WHEN пользователь вводит название события, THEN Система ДОЛЖНА запросить описание события с inline-кнопкой "⏭️ Пропустить"
5. WHEN пользователь вводит описание или пропускает его, THEN Система ДОЛЖНА сохранить событие в PostgreSQL с привязкой к пользователю и семье
6. WHEN событие сохранено, THEN Система ДОЛЖНА отправить подтверждение с деталями события
7. WHEN inline-календарь отображается, THEN Система ДОЛЖНА показывать текущий месяц с кнопками навигации для перехода к следующему/предыдущему месяцу
8. WHEN пользователь выбирает дату в прошлом, THEN Система ДОЛЖНА отклонить выбор и показать сообщение об ошибке
9. IF сохранение не удалось, THEN Система ДОЛЖНА логировать ошибку и уведомить пользователя

### Требование 5

**Пользовательская история:** Как член семьи, я хочу просмотреть все ближайшие события в календаре, чтобы знать планы семьи.

#### Критерии приемки

1. WHEN пользователь отправляет команду /upcoming_events, THEN Система ДОЛЖНА запросить из PostgreSQL все события семьи на ближайшие 7 дней
2. WHEN события найдены, THEN Система ДОЛЖНА отформатировать список с датой, временем, описанием и автором события
3. WHEN событий нет, THEN Система ДОЛЖНА отправить сообщение об отсутствии запланированных событий
4. WHEN список формируется, THEN Система ДОЛЖНА сортировать события по дате и времени
5. WHEN список отправляется, THEN Система ДОЛЖНА использовать Markdown форматирование для улучшения читаемости
6. WHEN события загружаются из базы данных, THEN Система ДОЛЖНА загружать все необходимые связанные сущности (User, Family) в рамках одной транзакции для предотвращения LazyInitializationException

### Требование 6

**Пользовательская история:** Как член семьи, я хочу получать уведомления о предстоящих событиях, чтобы не забыть о важных планах.

#### Критерии приемки

1. WHEN наступает время за 1 час до события, THEN Система ДОЛЖНА отправить уведомление всем членам семьи
2. WHEN уведомление формируется, THEN Система ДОЛЖНА включать дату, время, описание и автора события
3. WHEN уведомление отправлено, THEN Система ДОЛЖНА отметить в базе данных, что уведомление было отправлено
4. WHEN происходит ошибка отправки, THEN Система ДОЛЖНА повторить попытку до 3 раз
5. WHERE используется планировщик, Система ДОЛЖНА проверять предстоящие события каждые 5 минут

### Требование 7

**Пользовательская история:** Как член семьи, я хочу редактировать или удалять свои события, чтобы актуализировать календарь при изменении планов.

#### Критерии приемки

1. WHEN пользователь отправляет команду /my_events, THEN Система ДОЛЖНА показать список его событий с кнопками для редактирования и удаления
2. WHEN пользователь выбирает редактирование, THEN Система ДОЛЖНА позволить изменить дату, время или описание
3. WHEN пользователь выбирает удаление, THEN Система ДОЛЖНА запросить подтверждение перед удалением из базы
4. WHEN изменения сохранены, THEN Система ДОЛЖНА обновить запись в PostgreSQL и отправить подтверждение
5. WHEN пользователь пытается редактировать чужое событие, THEN Система ДОЛЖНА отклонить операцию с сообщением об ошибке

### Требование 8

**Пользовательская история:** Как разработчик, я хочу использовать Webhook для получения обновлений от Telegram, чтобы бот работал в реальном времени без постоянного опроса API.

#### Критерии приемки

1. WHEN приложение запускается, THEN Система ДОЛЖНА зарегистрировать Webhook URL в Telegram API
2. WHEN Telegram отправляет обновление на Webhook, THEN Система ДОЛЖНА обработать его асинхронно через REST controller
3. WHEN Webhook endpoint получает запрос, THEN Система ДОЛЖНА валидировать подпись запроса для безопасности
4. WHEN обработка завершена, THEN Система ДОЛЖНА вернуть HTTP 200 OK в течение 60 секунд
5. IF регистрация Webhook не удалась, THEN Система ДОЛЖНА логировать ошибку и остановить запуск

### Требование 9

**Пользовательская история:** Как разработчик, я хочу иметь централизованную обработку ошибок и логирование, чтобы все исключения обрабатывались единообразно.

#### Критерии приемки

1. WHEN происходит исключение в обработчике команды, THEN Система ДОЛЖНА перехватывать его и логировать с полным stack trace
2. WHEN ошибка перехвачена, THEN Система ДОЛЖНА отправлять пользователю дружественное сообщение об ошибке
3. WHEN происходит ошибка базы данных, THEN Система ДОЛЖНА логировать детали и уведомлять пользователя о временной недоступности
4. WHEN Telegram API возвращает ошибку, THEN Система ДОЛЖНА обрабатывать различные коды ошибок (rate limit, invalid token, etc.)
5. WHERE используется production окружение, Система ДОЛЖНА использовать структурированное логирование (JSON format)

### Требование 10

**Пользовательская история:** Как DevOps инженер, я хочу развернуть приложение с помощью Docker Compose, чтобы упростить установку и масштабирование.

#### Критерии приемки

1. WHEN выполняется docker-compose up, THEN Система ДОЛЖНА запустить контейнеры приложения и PostgreSQL
2. WHEN контейнеры запущены, THEN Система ДОЛЖНА автоматически применить миграции базы данных через Flyway или Liquibase
3. WHEN приложение в контейнере, THEN Система ДОЛЖНА использовать переменные окружения для конфигурации
4. WHEN PostgreSQL контейнер запущен, THEN Система ДОЛЖНА создать базу данных и пользователя автоматически
5. WHERE требуется персистентность, Система ДОЛЖНА использовать Docker volumes для хранения данных PostgreSQL

### Требование 11

**Пользовательская история:** Как разработчик, я хочу иметь миграции базы данных, чтобы схема БД версионировалась и применялась автоматически.

#### Критерии приемки

1. WHEN приложение запускается, THEN Система ДОЛЖНА автоматически применять миграции через Flyway или Liquibase
2. WHEN создается таблица users, THEN Система ДОЛЖНА включать поля: id, telegram_id (unique), username, first_name, family_id, created_at
3. WHEN создается таблица events, THEN Система ДОЛЖНА включать поля: id, user_id (FK), family_id, title, description, event_date, event_time, status, notified, created_at
4. WHEN создается таблица families, THEN Система ДОЛЖНА включать поля: id, name, created_at
5. WHEN миграция не удалась, THEN Система ДОЛЖНА остановить запуск и залогировать причину
6. WHEN поле status в таблице events создается, THEN Система ДОЛЖНА использовать ENUM или VARCHAR со значениями 'draft' и 'active', с default значением 'active'

### Требование 12

**Пользовательская история:** Как разработчик, я хочу иметь comprehensive документацию, чтобы новые разработчики могли быстро понять проект и развернуть его локально.

#### Критерии приемки

1. WHEN проект создается, THEN Система ДОЛЖНА включать README.md с описанием проекта, архитектуры и функционала
2. WHEN документация пишется, THEN Система ДОЛЖНА включать инструкции по локальному запуску с Docker Compose
3. WHEN описывается API, THEN Система ДОЛЖНА документировать все команды бота с примерами использования
4. WHEN описывается БД, THEN Система ДОЛЖНА включать ER-диаграмму и описание таблиц
5. WHERE требуется настройка Webhook, Система ДОЛЖНА предоставлять инструкции по настройке ngrok для локальной разработки

### Требование 13

**Пользовательская история:** Как член семьи, я хочу видеть доступные команды в виде кнопок в интерфейсе Telegram, чтобы не вводить команды вручную каждый раз.

#### Критерии приемки

1. WHEN пользователь открывает чат с ботом, THEN Система ДОЛЖНА отображать ReplyKeyboardMarkup с кнопками основных команд
2. WHEN пользователь нажимает на кнопку команды, THEN Система ДОЛЖНА обработать её так же, как текстовую команду
3. WHEN пользователь авторизован, THEN Система ДОЛЖНА показывать полный набор кнопок: "📅 Предстоящие события", "➕ Добавить событие", "📋 Мои события", "❓ Помощь"
4. WHEN пользователь не авторизован, THEN Система ДОЛЖНА показывать только кнопки: "🚀 Начать", "❓ Помощь"
5. WHEN клавиатура создается, THEN Система ДОЛЖНА использовать resize_keyboard=true для удобства использования

### Требование 14

**Пользовательская история:** Как член семьи, я хочу использовать inline кнопки для управления событиями, чтобы интерфейс был более интерактивным и современным.

#### Критерии приемки

1. WHEN пользователь просматривает свои события, THEN Система ДОЛЖНА отображать InlineKeyboardMarkup с кнопками "✏️ Редактировать" и "🗑️ Удалить" под каждым событием
2. WHEN пользователь нажимает inline кнопку, THEN Система ДОЛЖНА обработать callback query и выполнить соответствующее действие
3. WHEN пользователь выбирает удаление события, THEN Система ДОЛЖНА показать inline кнопки подтверждения: "✅ Да, удалить" и "❌ Отмена"
4. WHEN inline кнопка обрабатывается, THEN Система ДОЛЖНА отправить answerCallbackQuery для визуальной обратной связи
5. WHEN callback data формируется, THEN Система ДОЛЖНА включать идентификатор события для корректной обработки действий

### Требование 15

**Пользовательская история:** Как разработчик, я хочу управлять состоянием многошагового диалога создания события, чтобы система корректно обрабатывала последовательность действий пользователя.

#### Критерии приемки

1. WHEN пользователь начинает создание события, THEN Система ДОЛЖНА создать черновик события в таблице events со статусом "draft"
2. WHEN пользователь выбирает дату через inline-календарь, THEN Система ДОЛЖНА обновить черновик события с выбранной датой
3. WHEN пользователь выбирает время, THEN Система ДОЛЖНА обновить черновик события с выбранным временем
4. WHEN пользователь отправляет название события, THEN Система ДОЛЖНА обновить черновик с названием
5. WHEN пользователь отправляет описание или пропускает его, THEN Система ДОЛЖНА изменить статус черновика на "active" и завершить создание события
6. WHEN диалог отменяется, THEN Система ДОЛЖНА удалить черновик события из базы данных
7. WHEN пользователь начинает новый диалог, THEN Система ДОЛЖНА удалить предыдущий незавершенный черновик для данного пользователя

### Требование 16

**Пользовательская история:** Как член семьи, я хочу видеть улучшенный inline-календарь с визуальными индикаторами занятых дней и без возможности листать в прошлое, чтобы интерфейс был более информативным и удобным.

#### Критерии приемки

1. WHEN inline-календарь отображает дни в прошлом, THEN Система ДОЛЖНА показывать пустые ячейки без текста вместо точек
2. WHEN пользователь просматривает календарь, THEN Система ДОЛЖНА блокировать кнопку навигации "Предыдущий месяц" если предыдущий месяц находится в прошлом относительно текущего месяца
3. WHEN inline-календарь отображает дни с уже созданными событиями, THEN Система ДОЛЖНА визуально выделять эти дни добавляя эмодзи "📌" и инициал имени создателя события в верхнем правом углу (например, "5📌А" для события, созданного Алексеем)
4. WHEN Система проверяет наличие событий для календаря, THEN Система ДОЛЖНА запрашивать из базы данных все события семьи пользователя за отображаемый месяц
5. WHEN inline-календарь формируется, THEN Система ДОЛЖНА добавлять визуальный индикатор к номеру дня если на этот день есть хотя бы одно активное событие, включая инициал первого создателя события
6. WHEN на один день приходится несколько событий от разных пользователей, THEN Система ДОЛЖНА отображать инициал создателя первого события по времени


### Требование 17

**Пользовательская история:** Как член семьи, я хочу просматривать события на выбранную дату в календаре, чтобы видеть все запланированные и прошедшие мероприятия.

#### Критерии приемки

1. WHEN пользователь нажимает на дату в inline-календаре с существующими событиями, THEN Система ДОЛЖНА отобразить список всех событий на эту дату с деталями
2. WHEN дата находится в прошлом, THEN Система ДОЛЖНА показать только завершенные события без возможности создания нового
3. WHEN дата является текущей или будущей И на ней уже есть события, THEN Система ДОЛЖНА показать inline-кнопки "📋 Посмотреть события" и "➕ Создать новое"
4. WHEN пользователь выбирает "Посмотреть события", THEN Система ДОЛЖНА отобразить список событий с inline-кнопками для управления
5. WHEN пользователь выбирает "Создать новое", THEN Система ДОЛЖНА начать процесс создания события с предзаполненной датой

### Требование 18

**Пользовательская история:** Как член семьи, я хочу редактировать события через удобный интерфейс, чтобы быстро вносить изменения в планы.

#### Критерии приемки

1. WHEN пользователь нажимает кнопку "✏️ Редактировать" у события, THEN Система ДОЛЖНА показать inline-меню с опциями: "📅 Изменить дату", "🕐 Изменить время", "📝 Изменить название", "📄 Изменить описание"
2. WHEN пользователь выбирает изменение даты, THEN Система ДОЛЖНА показать inline-календарь для выбора новой даты
3. WHEN пользователь выбирает изменение времени, THEN Система ДОЛЖНА показать inline-кнопки для выбора нового времени
4. WHEN пользователь выбирает изменение названия или описания, THEN Система ДОЛЖНА запросить ввод нового текста
5. WHEN изменения применены, THEN Система ДОЛЖНА сохранить их в базе данных и записать в историю изменений
6. WHEN событие является частью серии повторений, THEN Система ДОЛЖНА спросить через inline-кнопки: "Изменить только это событие" или "Изменить всю серию"

### Требование 19

**Пользовательская история:** Как член семьи, я хочу удалять события с возможностью восстановления, чтобы не потерять важную информацию при случайном удалении.

#### Критерии приемки

1. WHEN пользователь подтверждает удаление события, THEN Система ДОЛЖНА переместить событие в корзину вместо окончательного удаления
2. WHEN событие перемещено в корзину, THEN Система ДОЛЖНА установить статус "deleted" и сохранить дату удаления
3. WHEN событие является частью серии повторений, THEN Система ДОЛЖНА спросить через inline-кнопки: "Удалить только это событие" или "Удалить всю серию"
4. WHEN пользователь открывает корзину через команду /trash, THEN Система ДОЛЖНА показать список удаленных событий с кнопками "♻️ Восстановить" и "🗑️ Удалить навсегда"
5. WHEN пользователь восстанавливает событие, THEN Система ДОЛЖНА вернуть статус "active" и очистить дату удаления
6. WHEN событие в корзине старше 30 дней, THEN Система ДОЛЖНА автоматически удалить его окончательно

### Требование 20

**Пользовательская история:** Как член семьи, я хочу прикреплять файлы к событиям, чтобы хранить билеты, документы и изображения в одном месте.

#### Критерии приемки

1. WHEN пользователь создает или редактирует событие, THEN Система ДОЛЖНА предложить через inline-кнопку "📎 Прикрепить файл"
2. WHEN пользователь отправляет файл, документ или изображение, THEN Система ДОЛЖНА сохранить его в базе данных с привязкой к событию
3. WHEN файл сохранен, THEN Система ДОЛЖНА сохранить file_id от Telegram, имя файла, тип и размер
4. WHEN пользователь просматривает событие с вложениями, THEN Система ДОЛЖНА показать список файлов с inline-кнопками для просмотра
5. WHEN пользователь нажимает на вложение, THEN Система ДОЛЖНА отправить файл пользователю
6. WHEN размер файла превышает 20 МБ, THEN Система ДОЛЖНА отклонить загрузку с сообщением об ограничении

### Требование 21

**Пользовательская история:** Как член семьи, я хочу добавлять комментарии к событиям, чтобы обсуждать детали с другими членами семьи.

#### Критерии приемки

1. WHEN пользователь просматривает событие, THEN Система ДОЛЖНА показать inline-кнопку "💬 Добавить комментарий"
2. WHEN пользователь нажимает кнопку комментария, THEN Система ДОЛЖНА запросить ввод текста комментария
3. WHEN комментарий отправлен, THEN Система ДОЛЖНА сохранить его с указанием автора и времени создания
4. WHEN пользователь просматривает событие с комментариями, THEN Система ДОЛЖНА отобразить все комментарии с именами авторов и временем
5. WHEN добавлен новый комментарий к семейному событию, THEN Система ДОЛЖНА отправить уведомление всем членам семьи

### Требование 22

**Пользовательская история:** Как член семьи, я хочу создавать чек-листы внутри событий, чтобы отслеживать выполнение задач.

#### Критерии приемки

1. WHEN пользователь создает или редактирует событие, THEN Система ДОЛЖНА предложить через inline-кнопку "☑️ Добавить чек-лист"
2. WHEN пользователь добавляет чек-лист, THEN Система ДОЛЖНА запросить ввод пунктов списка (по одному на строку)
3. WHEN чек-лист создан, THEN Система ДОЛЖНА сохранить все пункты с флагом completed=false
4. WHEN пользователь просматривает событие с чек-листом, THEN Система ДОЛЖНА показать все пункты с inline-кнопками для отметки выполнения
5. WHEN пользователь отмечает пункт выполненным, THEN Система ДОЛЖНА обновить флаг completed=true и показать визуальный индикатор ✅
6. WHEN все пункты чек-листа выполнены, THEN Система ДОЛЖНА показать сообщение о завершении всех задач

### Требование 23

**Пользовательская история:** Как член семьи, я хочу настраивать гибкие напоминания для событий, чтобы получать уведомления в удобное время.

#### Критерии приемки

1. WHEN пользователь создает или редактирует событие, THEN Система ДОЛЖНА показать inline-кнопку "🔔 Настроить напоминания"
2. WHEN пользователь нажимает кнопку настройки, THEN Система ДОЛЖНА показать inline-меню с опциями: "Утром в день события", "Вечером накануне", "За 1 час", "За 10 минут", "Свое время"
3. WHEN пользователь выбирает несколько вариантов, THEN Система ДОЛЖНА сохранить все выбранные напоминания для события
4. WHEN наступает время напоминания, THEN Система ДОЛЖНА отправить уведомление согласно настройкам
5. WHEN пользователь выбирает "Свое время", THEN Система ДОЛЖНА запросить ввод времени в формате "за X минут/часов/дней"
6. WHEN напоминание отправлено, THEN Система ДОЛЖНА отметить его как отправленное в базе данных

### Требование 24

**Пользовательская история:** Как член семьи, я хочу получать контекстные подсказки при создании события, чтобы не забыть важные детали.

#### Критерии приемки

1. WHEN пользователь создает событие с ключевыми словами "день рождения", "праздник", THEN Система ДОЛЖНА предложить через inline-кнопку "🎁 Добавить список подарков"
2. WHEN пользователь создает событие с ключевыми словами "поездка", "путешествие", "полет", THEN Система ДОЛЖНА предложить "🎫 Прикрепить билеты"
3. WHEN пользователь создает событие с ключевыми словами "встреча", "собрание", THEN Система ДОЛЖНА предложить "📋 Создать повестку дня"
4. WHEN пользователь принимает подсказку, THEN Система ДОЛЖНА автоматически создать соответствующий чек-лист или запросить вложение
5. WHEN подсказка отклонена, THEN Система ДОЛЖНА продолжить создание события без дополнительных действий

### Требование 25

**Пользовательская история:** Как член семьи, я хочу, чтобы события автоматически завершались после окончания, чтобы календарь оставался актуальным.

#### Критерии приемки

1. WHEN текущее время превышает время окончания события, THEN Система ДОЛЖНА автоматически изменить статус на "completed"
2. WHEN событие завершено, THEN Система ДОЛЖНА отправить создателю inline-кнопку "📝 Добавить заметку о событии"
3. WHEN пользователь добавляет заметку к завершенному событию, THEN Система ДОЛЖНА сохранить её с меткой времени
4. WHEN пользователь просматривает завершенное событие, THEN Система ДОЛЖНА показать заметки и возможность добавить новые
5. WHERE используется планировщик, Система ДОЛЖНА проверять события для завершения каждые 10 минут

### Требование 26

**Пользовательская история:** Как член семьи, я хочу создавать персональные события, которые видны только мне, чтобы не загромождать общий семейный календарь.

#### Критерии приемки

1. WHEN пользователь начинает создание события, THEN Система ДОЛЖНА показать inline-кнопки выбора типа: "👨‍👩‍👧‍👦 Семейное" и "👤 Персональное"
2. WHEN пользователь выбирает "Персональное", THEN Система ДОЛЖНА установить флаг is_personal=true для события
3. WHEN персональное событие сохранено, THEN Система ДОЛЖНА сделать его видимым только для создателя
4. WHEN пользователь просматривает календарь, THEN Система ДОЛЖНА показывать семейные события всем членам семьи, а персональные только создателю
5. WHEN формируется список предстоящих событий, THEN Система ДОЛЖНА включать персональные события только для их создателя
6. WHEN отправляются напоминания о персональном событии, THEN Система ДОЛЖНА отправлять их только создателю

### Требование 27

**Пользовательская история:** Как член семьи, я хочу создавать повторяющиеся события с гибкими настройками, чтобы не вводить одинаковые события вручную.

#### Критерии приемки

1. WHEN пользователь создает событие, THEN Система ДОЛЖНА показать inline-кнопку "🔁 Настроить повторение"
2. WHEN пользователь нажимает кнопку повторения, THEN Система ДОЛЖНА показать меню: "Каждый день", "Каждую неделю", "Каждый месяц", "Свой интервал"
3. WHEN пользователь выбирает "Свой интервал", THEN Система ДОЛЖНА запросить ввод интервала (например, "каждые 3 дня", "каждые 2 недели")
4. WHEN пользователь настраивает повторение по дням недели, THEN Система ДОЛЖНА показать inline-кнопки для выбора дней (Пн, Вт, Ср, Чт, Пт, Сб, Вс)
5. WHEN пользователь настраивает ограничение повторений, THEN Система ДОЛЖНА предложить выбор: "До определенной даты" или "Количество повторений"
6. WHEN повторяющееся событие создано, THEN Система ДОЛЖНА сохранить правило повторения и создать связь между событиями серии
7. WHEN пользователь редактирует событие из серии, THEN Система ДОЛЖНА спросить: "Изменить только это" или "Изменить всю серию"
8. WHEN пользователь удаляет событие из серии, THEN Система ДОЛЖНА спросить: "Удалить только это" или "Удалить всю серию"
9. WHEN пользователь создает исключение для даты в серии, THEN Система ДОЛЖНА пропустить создание события на эту дату

### Требование 28

**Пользовательская история:** Как член семьи, я хочу просматривать события за разные периоды времени, чтобы планировать свое расписание.

#### Критерии приемки

1. WHEN пользователь отправляет команду /today, THEN Система ДОЛЖНА показать все события на текущий день
2. WHEN пользователь отправляет команду /week, THEN Система ДОЛЖНА показать все события на текущую неделю с группировкой по дням
3. WHEN пользователь отправляет команду /search, THEN Система ДОЛЖНА запросить текст для поиска по названию и описанию событий
4. WHEN пользователь вводит текст поиска, THEN Система ДОЛЖНА найти все события, содержащие этот текст, и отобразить результаты
5. WHEN пользователь отправляет команду /filter, THEN Система ДОЛЖНА показать inline-меню фильтров: "Семейные", "Персональные", "Завершенные", "Предстоящие"
6. WHERE используется еженедельная сводка, Система ДОЛЖНА отправлять каждое воскресенье вечером список событий на следующую неделю

### Требование 29

**Пользовательская история:** Как член семьи, я хочу видеть историю изменений событий, чтобы отслеживать, кто и когда вносил правки.

#### Критерии приемки

1. WHEN событие изменяется, THEN Система ДОЛЖНА создать запись в таблице event_history с деталями изменения
2. WHEN запись истории создается, THEN Система ДОЛЖНА сохранить: event_id, user_id, action_type, old_value, new_value, changed_at
3. WHEN пользователь просматривает событие, THEN Система ДОЛЖНА показать inline-кнопку "📜 История изменений"
4. WHEN пользователь открывает историю, THEN Система ДОЛЖНА показать список всех изменений с именами авторов и временем
5. WHEN отображается изменение, THEN Система ДОЛЖНА показать, что именно было изменено (дата, время, описание и т.д.)

### Требование 30

**Пользовательская история:** Как член семьи, я хочу быстро создавать события из текстового сообщения, чтобы экономить время.

#### Критерии приемки

1. WHEN пользователь отправляет текстовое сообщение в формате "Событие: [название] Дата: [дата] Время: [время]", THEN Система ДОЛЖНА распознать параметры и предложить создать событие
2. WHEN параметры распознаны, THEN Система ДОЛЖНА показать inline-кнопку "✅ Создать событие" с предпросмотром параметров
3. WHEN пользователь подтверждает создание, THEN Система ДОЛЖНА создать событие с распознанными параметрами
4. WHEN формат сообщения некорректен, THEN Система ДОЛЖНА предложить использовать стандартный процесс создания
5. WHEN событие создано из текста, THEN Система ДОЛЖНА запросить подтверждение всех параметров перед сохранением

### Требование 31

**Пользовательская история:** Как член семьи, я хочу видеть улучшенный календарь с подсветкой текущей даты и статистикой, чтобы лучше ориентироваться в событиях.

#### Критерии приемки

1. WHEN inline-календарь отображается, THEN Система ДОЛЖНА выделять текущую дату специальным эмодзи "📍" или цветом
2. WHEN пользователь просматривает календарь, THEN Система ДОЛЖНА показывать мини-статистику: "События в этом месяце: X"
3. WHEN пользователь запрашивает статистику через команду /stats, THEN Система ДОЛЖНА показать: количество событий за месяц, количество завершенных событий, количество предстоящих
4. WHEN отображается статистика, THEN Система ДОЛЖНА группировать данные по типам событий (семейные/персональные)
5. WHEN календарь показывает день с несколькими событиями, THEN Система ДОЛЖНА добавлять счетчик событий к дате (например, "5📌А(3)" для 3 событий)

### Требование 32

**Пользовательская история:** Как член семьи, я хочу создавать события с временным интервалом, чтобы указывать продолжительность мероприятий.

#### Критерии приемки

1. WHEN пользователь создает событие и выбирает время начала, THEN Система ДОЛЖНА предложить через inline-кнопку "⏱️ Указать время окончания"
2. WHEN пользователь указывает время окончания, THEN Система ДОЛЖНА сохранить оба времени в базе данных
3. WHEN время окончания раньше времени начала, THEN Система ДОЛЖНА показать ошибку и запросить корректное время
4. WHEN событие отображается, THEN Система ДОЛЖНА показывать интервал в формате "10:00 - 12:00"
5. WHEN событие с интервалом завершается, THEN Система ДОЛЖНА использовать время окончания для определения статуса "completed"
