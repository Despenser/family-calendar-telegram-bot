# Implementation Plan: Event Completion Empty State Timing

## Overview

Данный план описывает задачи для исправления проблемы с преждевременным отображением сообщения "У вас пока нет созданных событий" при завершении последнего события в списке с добавлением заметки о завершении.

## Tasks

- [x] 1. Добавить новый метод completeEventWithoutHeaderUpdate() в EventService
  - Скопировать логику из `completeEventWithoutDeletion()`
  - Удалить вызов `updateMyEventsHeaderAfterRemoval()`
  - Добавить JavaDoc с описанием назначения метода
  - _Requirements: 1.1, 2.1, 2.2_

- [ ]* 1.1 Написать unit-тесты для completeEventWithoutHeaderUpdate()
  - Проверить, что метод НЕ вызывает `updateMyEventsHeaderAfterRemoval()`
  - Проверить, что событие корректно завершается
  - Проверить, что messageId сохраняется
  - Проверить обработку исключений
  - _Requirements: 1.1, 2.1_

- [x] 2. Обновить EventCallbackHandler.handleCompleteEvent()
  - Заменить вызов `completeEventWithoutDeletion()` на `completeEventWithoutHeaderUpdate()`
  - Обновить комментарии и JavaDoc
  - _Requirements: 1.1, 1.2_

- [ ]* 2.1 Написать unit-тесты для handleCompleteEvent()
  - Проверить использование нового метода
  - Проверить, что сообщение о пустом списке НЕ отправляется сразу
  - Проверить установку контекста ожидания заметки
  - _Requirements: 1.1, 1.2_

- [x] 3. Обновить EventCallbackHandler.handleSkipCompletionNote()
  - Добавить вызов `eventService.updateMyEventsHeaderAfterRemoval(userId)` ПОСЛЕ отображения карточки события
  - Обновить комментарии и JavaDoc
  - _Requirements: 1.4, 3.3_

- [ ]* 3.1 Написать unit-тесты для handleSkipCompletionNote()
  - Проверить вызов `updateMyEventsHeaderAfterRemoval()` в правильном месте
  - Проверить последовательность операций
  - Проверить обработку ошибок
  - _Requirements: 1.4, 3.3_

- [x] 4. Обновить UpdateProcessor для обработки заметок о завершении
  - Найти обработку состояния `isAwaitingCompletionNote`
  - Добавить вызов `eventService.updateMyEventsHeaderAfterRemoval(userId)` ПОСЛЕ добавления заметки и отображения финального сообщения
  - Обновить комментарии
  - _Requirements: 1.2, 1.3, 3.1_

- [ ]* 4.1 Написать unit-тесты для обработки заметок в UpdateProcessor
  - Проверить вызов `updateMyEventsHeaderAfterRemoval()` после добавления заметки
  - Проверить последовательность сообщений
  - Проверить обработку ошибок
  - _Requirements: 1.2, 1.3, 3.1_

- [x] 5. Checkpoint - Убедиться, что все тесты проходят
  - Запустить все unit-тесты
  - Проверить отсутствие регрессий
  - Исправить найденные проблемы

- [ ]* 6. Написать интеграционный тест для полного сценария
  - Тест завершения последнего события с добавлением заметки
  - Проверить, что сообщение о пустом списке отправляется ПОСЛЕ ввода заметки
  - Тест завершения последнего события с пропуском заметки
  - Проверить, что сообщение о пустом списке отправляется сразу после пропуска
  - Тест завершения события при наличии других событий
  - Проверить, что сообщение о пустом списке НЕ отправляется
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5, 3.1, 3.3_

- [ ] 7. Финальный checkpoint - Убедиться, что все тесты проходят
  - Запустить все тесты (unit + integration)
  - Проверить корректность работы в различных сценариях
  - Убедиться в отсутствии регрессий

## Notes

- Задачи, отмеченные `*`, являются опциональными и могут быть пропущены для более быстрой реализации MVP
- Каждая задача ссылается на конкретные требования для отслеживаемости
- Checkpoint задачи обеспечивают инкрементальную валидацию
- Основное изменение - добавление нового метода и изменение порядка вызовов существующих методов
- Обратная совместимость сохраняется, так как существующий метод `completeEventWithoutDeletion()` не изменяется
