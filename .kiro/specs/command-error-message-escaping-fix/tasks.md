# Implementation Plan: Command Error Message Escaping Fix

## Overview

План реализации исправления экранирования специальных символов MarkdownV2 в сообщениях об ошибках команд. Исправления будут внесены в два файла: `UpdateProcessor.java` и `CommandDispatcher.java`.

## Tasks

- [x] 1. Исправить UpdateProcessor для использования formatMessage()
  - Найти строку 1635-1637 в UpdateProcessor.java
  - Заменить конкатенацию строк с escape() на MarkdownFormatter.formatMessage()
  - Убедиться, что сообщение формируется корректно
  - Проверить, что импорт MarkdownFormatter присутствует
  - _Requirements: 1.1, 1.2, 1.4, 2.4_

- [ ]* 1.1 Написать unit тест для UpdateProcessor
  - Создать тест для обработки некорректной команды (текст без '/')
  - Проверить, что сообщение об ошибке корректно экранировано
  - Проверить, что все специальные символы (точки, апострофы) экранированы
  - Использовать mock объекты для зависимостей
  - _Requirements: 1.1, 1.2, 3.1_

- [x] 2. Исправить CommandDispatcher для использования formatMessage()
  - Найти строку 157 в CommandDispatcher.java
  - Заменить конкатенацию строк с escape() на MarkdownFormatter.formatMessage()
  - Убедиться, что метод возвращает корректно экранированное сообщение
  - Проверить, что импорт MarkdownFormatter присутствует
  - _Requirements: 1.1, 1.2, 1.4, 2.4_

- [ ]* 2.1 Написать unit тест для CommandDispatcher
  - Создать тест для обработки некорректной команды
  - Проверить, что возвращаемое сообщение корректно экранировано
  - Проверить, что команда /help отображается правильно
  - Использовать mock объекты для зависимостей
  - _Requirements: 1.1, 1.3, 3.2_

- [x] 3. Checkpoint - Убедиться, что все тесты проходят
  - Ensure all tests pass, ask the user if questions arise.

- [ ]* 4. Написать property-based тест для полного экранирования
  - **Property 1: Полное экранирование сообщения об ошибке для некорректного ввода**
  - **Validates: Requirements 1.1**
  - Использовать jqwik для генерации случайных строк, не начинающихся с '/'
  - Проверять, что все специальные символы в сообщении экранированы
  - Минимум 100 итераций теста
  - _Requirements: 1.1, 3.4_

- [x] 5. Проверить другие места в коде на аналогичные проблемы
  - Выполнить поиск конкатенации с escape() в коде
  - Выполнить поиск String.format() для сообщений
  - Проанализировать найденные места на наличие проблем
  - Исправить найденные проблемы (если есть)
  - _Requirements: 2.4_

- [x] 6. Final Checkpoint - Убедиться, что все тесты проходят
  - Ensure all tests pass, ask the user if questions arise.

## Notes

- Задачи, отмеченные `*`, являются опциональными и могут быть пропущены для более быстрого MVP
- Каждая задача ссылается на конкретные требования для отслеживаемости
- Checkpoints обеспечивают инкрементальную валидацию
- Property-based тесты валидируют универсальные свойства корректности
- Unit тесты валидируют конкретные примеры и граничные случаи
