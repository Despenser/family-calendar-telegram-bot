# Implementation Plan: Remove Bell Icon from Event Lists

## Overview

Данный план описывает задачи для удаления отображения эмодзи колокольчика (🔔) из списков событий в командах `/today`, `/week` и `/upcoming_events`. Реализация фокусируется на минимальных изменениях в классе `EventFormatter` с сохранением обратной совместимости.

## Tasks

- [x] 1. Удалить отображение колокольчика в EventFormatter
  - Открыть файл `src/main/java/ru/golubyatnikov/family/calendar/bot/util/EventFormatter.java`
  - Найти метод `formatEvent(Event event, User currentUser, boolean hasReminders)` (строки 110-115)
  - Удалить условие `if (hasReminders)` и строку `sb.append(escape(" 🔔"));`
  - Обновить комментарий для документирования изменения
  - Параметр `hasReminders` остаётся в сигнатуре метода для обратной совместимости
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5, 3.1, 3.2_

- [ ]* 1.1 Написать unit тесты для EventFormatter
  - Создать тестовый класс `EventFormatterTest.java` в `src/test/java/ru/golubyatnikov/family/calendar/bot/util/`
  - Тест 1: Проверить отсутствие колокольчика при `hasReminders = true`
  - Тест 2: Проверить отсутствие колокольчика при `hasReminders = false`
  - Тест 3: Проверить обратную совместимость API (оба вызова выполняются без ошибок)
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5, 3.1, 3.2_

- [ ]* 1.2 Написать property тест для отсутствия колокольчика
  - **Property 1: Отсутствие колокольчика в списках событий**
  - **Validates: Requirements 1.1, 1.2, 1.3, 1.4, 1.5**
  - Создать тестовый класс `EventFormatterPropertyTest.java`
  - Генерировать случайные события с разными параметрами
  - Генерировать случайные значения `hasReminders` (true/false)
  - Для каждой комбинации вызывать `formatEvent()` и проверять отсутствие "🔔"
  - Минимум 100 итераций
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5_

- [ ]* 1.3 Написать property тест для обратной совместимости
  - **Property 3: Обратная совместимость API**
  - **Validates: Requirements 3.1, 3.2**
  - Добавить в `EventFormatterPropertyTest.java`
  - Генерировать случайные события
  - Для каждого события вызывать `formatEvent()` с `hasReminders = true` и `false`
  - Проверять, что оба вызова возвращают идентичные результаты
  - Проверять, что оба вызова выполняются без исключений
  - Минимум 100 итераций
  - _Requirements: 3.1, 3.2_

- [ ] 2. Checkpoint - Убедиться, что все тесты проходят
  - Запустить все тесты проекта: `mvn test`
  - Убедиться, что новые тесты проходят
  - Убедиться, что существующие тесты не сломались
  - Если есть проблемы, исправить их
  - Спросить пользователя, если возникают вопросы

- [ ]* 3. Написать интеграционные тесты для команд
  - Создать тестовый класс `EventListCommandsIntegrationTest.java`
  - Тест для `/today`: создать события с напоминаниями, проверить отсутствие "🔔"
  - Тест для `/week`: создать события с напоминаниями, проверить отсутствие "🔔"
  - Тест для `/upcoming_events`: создать события с напоминаниями, проверить отсутствие "🔔"
  - Тест для `/my_events`: создать событие с напоминаниями, проверить наличие "🔔" в детальном просмотре
  - _Requirements: 1.1, 1.2, 1.3, 2.1_

- [-] 4. Финальный checkpoint - Убедиться, что все тесты проходят
  - Запустить все тесты проекта: `mvn test`
  - Убедиться, что все тесты проходят
  - Проверить, что изменения не сломали существующую функциональность
  - Спросить пользователя, если возникают вопросы

## Notes

- Задачи, помеченные `*`, являются опциональными и могут быть пропущены для более быстрой реализации MVP
- Каждая задача ссылается на конкретные требования для отслеживаемости
- Checkpoint задачи обеспечивают инкрементальную валидацию
- Property тесты валидируют универсальные свойства корректности
- Unit тесты валидируют конкретные примеры и граничные случаи
- Интеграционные тесты проверяют работу команд end-to-end
