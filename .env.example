# =============================================================================
# ПРИМЕР ФАЙЛА ПЕРЕМЕННЫХ ОКРУЖЕНИЯ
# =============================================================================
# Скопируйте этот файл в .env и заполните реальными значениями
# ВАЖНО: Файл .env не должен попадать в систему контроля версий!
# =============================================================================

# -----------------------------------------------------------------------------
# Spring Boot Configuration
# -----------------------------------------------------------------------------
# Активный профиль: dev, prod
SPRING_PROFILES_ACTIVE=dev

# Порт сервера (по умолчанию 8080)
SERVER_PORT=8080

# -----------------------------------------------------------------------------
# PostgreSQL Database Configuration
# -----------------------------------------------------------------------------
# URL подключения к базе данных
# Формат: jdbc:postgresql://host:port/database_name
SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/family_calendar

# Имя пользователя БД
SPRING_DATASOURCE_USERNAME=botuser

# Пароль пользователя БД
SPRING_DATASOURCE_PASSWORD=your_secure_password_here

# Пароль для PostgreSQL (используется в docker-compose.yml)
DB_PASSWORD=your_secure_password_here

# -----------------------------------------------------------------------------
# Telegram Bot Configuration
# -----------------------------------------------------------------------------
# Токен бота от @BotFather
# Получить: https://t.me/BotFather -> /newbot
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz1234567890

# Имя пользователя бота (без @)
TELEGRAM_BOT_USERNAME=FamilyCalendarBot

# URL для Webhook (HTTPS обязателен!)
# Production: https://<ВАШ_ПУБЛИЧНЫЙ_IP>/webhook
# Development: https://your-ngrok-url.ngrok.io/webhook
TELEGRAM_BOT_WEBHOOK_URL=https://your-domain-or-ip.com/webhook

# -----------------------------------------------------------------------------
# GigaChat AI Configuration
# -----------------------------------------------------------------------------
# API ключ для GigaChat
# Получить: https://developers.sber.ru/portal/products/gigachat
GIGACHAT_API_KEY=your_gigachat_api_key_here

# Модель GigaChat (GigaChat-Max, GigaChat-Pro, GigaChat)
GIGACHAT_MODEL=GigaChat-Max

GIGACHAT_API_SCOPE=GIGACHAT_API_PERS

# Температура генерации (0.0 - 1.0, меньше = более детерминированно)
GIGACHAT_TEMPERATURE=0.3

# Максимальное количество токенов в ответе
GIGACHAT_MAX_TOKENS=1024

# -----------------------------------------------------------------------------
# Development Environment (опционально для dev профиля)
# -----------------------------------------------------------------------------
# Токен тестового бота для разработки
TELEGRAM_BOT_TOKEN_DEV=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz1234567890

# Имя тестового бота
TELEGRAM_BOT_USERNAME_DEV=FamilyCalendarDevBot

# Webhook URL для разработки (ngrok)
# Пример: https://abc123.ngrok.io/webhook
TELEGRAM_BOT_WEBHOOK_URL_DEV=https://your-ngrok-url.ngrok.io/webhook

# -----------------------------------------------------------------------------
# Logging Configuration (опционально)
# -----------------------------------------------------------------------------
# Уровень логирования: TRACE, DEBUG, INFO, WARN, ERROR
LOGGING_LEVEL_ROOT=INFO
LOGGING_LEVEL_APP=DEBUG

# -----------------------------------------------------------------------------
# Инструкции по настройке
# -----------------------------------------------------------------------------
# 1. Создайте бота через @BotFather в Telegram
# 2. Получите токен бота и сохраните его в TELEGRAM_BOT_TOKEN
# 3. Установите PostgreSQL или используйте Docker Compose
# 4. Создайте базу данных: CREATE DATABASE family_calendar;
# 5. Создайте пользователя БД: CREATE USER botuser WITH PASSWORD 'your_password';
# 6. Дайте права: GRANT ALL PRIVILEGES ON DATABASE family_calendar TO botuser;
#
# Для Production с HTTPS:
# 1. Сгенерируйте SSL сертификаты: ./scripts/unix/ssl.sh <ВАШ_IP> [DAYS]
# 2. Укажите webhook URL: TELEGRAM_BOT_WEBHOOK_URL=https://<ВАШ_IP>/webhook
# 3. Запустите: docker-compose up -d
# 4. Проверьте webhook: curl https://api.telegram.org/bot<TOKEN>/getWebhookInfo
#
# Для локальной разработки с ngrok:
# 1. Установите ngrok: brew install ngrok (macOS) или https://ngrok.com/download
# 2. Запустите туннель: ngrok http 8080
# 3. Скопируйте HTTPS URL в TELEGRAM_BOT_WEBHOOK_URL
# 4. Установите профиль: SPRING_PROFILES_ACTIVE=dev
# 5. Запустите: docker-compose up -d
#
# Подробная документация: docs/HTTPS_WEBHOOK_SETUP.md
# -----------------------------------------------------------------------------
